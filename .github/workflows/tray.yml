name: tray

on:
  push:
    branches: [main]
    paths: ["tray-go/**", ".github/workflows/tray*.yml"]
  pull_request:
    paths: ["tray-go/**"]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    uses: ./.github/workflows/tray-build.yml
